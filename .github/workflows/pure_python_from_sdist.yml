name: check pure python install from sdist
on:
   - push

jobs:
  build_sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install poetry
        run: pip install poetry
      - name: Build package
        run: poetry build
      - uses: actions/upload-artifact@v2
        # Upload the artifact so the MacOS runner do something with it
        with:
          name: Build_SDist
          path: dist/seamless_entropy-0.1.0.tar.gz

  install_and_test_sdist:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: test_packages/entropy_pure_python_from_sdist
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - uses: actions/download-artifact@master
        with:
          name: Build_SDist
          path: seamless_entropy-0.1.0.tar.gz
      - name: Install poetry
        run: pip install poetry
      - name: Install requirements
        run: poetry install
      - name: Install sdist package for seamless_entropy
        run: poetry run pip install seamless_entropy-0.1.0.tar.gz
      - name: Test package
        run: poetry run pytest 
